<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Sortir | {% endblock %}</title>
        <link href="{{ asset('https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css') }}"
              rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    </head>
    <body>
        <div class="container-fluid header-eni">
            <div class="row align-items-end">
                <div class="col">
                    <div class="h1">SORTIR.com</div>
                </div>
                {% if (app.user) %}
                <div class="col-8">
                    <ul class="nav text-uppercase justify-content-end">
                        {%  if (app.request.get('_route')!='main_user_edit') %}
                            <li class="nav-item"><a class="nav-link disabled" href="{{ path('main_home') }}">Villes</a></li>
                            <li class="nav-item"><a class="nav-link disabled" href="{{ path('main_home') }}">Campus</a></li>
                        {% endif %}
                        <li class="nav-item"><a class="nav-link active" href="{{ path('main_home') }}">Accueil</a></li>
                        <li class="nav-item"><a class="nav-link active" href="{{ path('main_user_edit') }}">Mon profil</a></li>
                        <li class="nav-item"><a class="nav-link active" href="{{ path('app_logout') }}">Se déconnecter</a></li>
                    </ul>
                </div>
                {% endif %}
            </div>
        </div>

        {% block body %}{% endblock %}
    </body>
</html>
